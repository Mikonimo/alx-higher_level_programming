#!/usr/bin/python3
def number_keys(a_dictionary):
    """
    Returns the number of keys in a dictionary
    Args:
    a_dictionary: the dictionary being evaluated
    Return:
    number of keys in the a_dictionary
    """
    return (len(a_dictionary))
